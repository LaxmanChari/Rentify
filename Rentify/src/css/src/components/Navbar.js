import React from 'react';

const Navbar = () => {
    return (
        <nav>
            <h2>Rentify</h2>
            {/* Navigation links */}
        </nav>
    );
};

export default Navbar;
