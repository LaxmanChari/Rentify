import React from 'react';

const PropertyList = () => {
    // Fetch property list from backend and render
    return (
        <div>
            <h2>Property List</h2>
            {/* Render property list */}
        </div>
    );
};

export default PropertyList;
